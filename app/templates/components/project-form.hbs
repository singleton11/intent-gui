{{#with (changeset model ProjectValidations) as |changeset|}}
  {{#each changeset.errors as |errorSet|}}
    {{#each errorSet.validation as |error|}}
      <div class="alert alert-danger" role="alert">
        {{error}}
      </div>
    {{/each}}
  {{/each}}
  <form>
    <div class="form-group">
      <label>TITLE</label>
      {{input value=changeset.title class='form-control'}}
      <small></small>
    </div>
    <div class="form-group">
      <label>DESCRIPTION</label>
      {{textarea value=changeset.description class='form-control'}}
    </div>
    <button type="submit" class="btn btn-primary" {{action 'save' changeset}}>SAVE</button>
  </form>
{{/with}}


<div class="modal fade" id="error" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Server error</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        There was error with data you try to commit
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
      </div>
    </div>
  </div>
</div>

{{yield}}
